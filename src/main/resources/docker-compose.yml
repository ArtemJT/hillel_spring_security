version: '3.3'

services:
  postgres_storeDB:
    image: postgres:14.6
    restart: always
    environment:
      POSTGRES_DB: testDB
      POSTGRES_PASSWORD: testDB
      POSTGRES_USER: testDB
    ports:
      - "5432:5432"
